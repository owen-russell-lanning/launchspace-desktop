<UserControl x:Class="launchspace_desktop.pages.NewLauncherPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:launchspace_desktop.pages" xmlns:components="clr-namespace:launchspace_desktop.components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.7*"></RowDefinition>
            <RowDefinition Height="0.12*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Margin="30, 20">
            <components:TextInput x:Name="nameInput" Padding="6"></components:TextInput>
            

        </StackPanel>
        <Grid  Grid.Row="1" Margin="15,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*"></ColumnDefinition>
                <ColumnDefinition Width="0.2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label x:Name="errorLabel" Foreground="Red" HorizontalAlignment="Left"></Label>
            <components:TextButton HorizontalAlignment="Right" x:Name="createButton" Grid.Column="1" ></components:TextButton>
        </Grid>
    </Grid>
</UserControl>
